import assert from 'node:assert/strict';
import * as T from '../game/vendor/three.module.js';
import {createCinematicPass} from '../game/src/cinematic-pass.js';
let target=null,draws=[],depthClears=0;const renderer={getRenderTarget:()=>target,setRenderTarget:t=>{target=t;},getDrawingBufferSize:v=>v.set(1400,900),render:(scene,camera)=>draws.push({scene,camera,target}),clearDepth:()=>depthClears++};
const cinema=createCinematicPass(renderer),scene=new T.Scene(),camera=new T.PerspectiveCamera(65,14/9,.05,3000);scene.background=new T.Color(0x202020);const background=scene.background;
function render(options={}){draws=[];depthClears=0;cinema.render(scene,camera,options);assert.equal(scene.background,background);assert.equal(camera.near,.05);assert.equal(camera.layers.mask,1);assert.equal(target,null);assert.equal(renderer.autoClear,true);return {calls:draws.length,world:draws.filter(x=>x.scene===scene).length,clears:depthClears};}
assert.deepEqual(render(),{calls:16,world:2,clears:1});assert.equal(cinema.stats().worldMsaaSamples,4);assert.equal(cinema.stats().bloomPasses,3);assert.equal(cinema.stats().contactOcclusionSamples,8);
const hdr=draws[0].target,depth=hdr.depthTexture,texture=hdr.texture;let disposals=0;hdr.addEventListener('dispose',()=>disposals++);
assert.deepEqual(render({reducedMotion:true}),{calls:16,world:2,clears:1});assert.equal(disposals,0,'motion preference alone keeps image quality');
assert.deepEqual(render({reducedEffects:true}),{calls:3,world:2,clears:1});
assert.equal(disposals,1,'parking unused 4x HDR frees the full-res MSAA buffer');
assert.equal(cinema.stats().worldMsaaSamples,0);assert.equal(cinema.stats().bloomPasses,0);assert.equal(cinema.stats().contactOcclusionSamples,0);
assert.equal(hdr.depthTexture,depth);assert.equal(hdr.texture,texture);assert.equal(hdr.samples,4);
assert.equal(hdr.width,1);assert.equal(hdr.height,1);
assert.notEqual(draws[0].target,hdr);assert.equal(draws[0].target.samples,0);
assert.equal(draws[0].target.width,350);assert.equal(draws[0].target.height,225);
assert.ok(draws[0].target.width/1400<=.5);
assert.equal(cinema.stats().liveWorldHdrWidth,350);assert.equal(cinema.stats().liveWorldScale,.25);assert.equal(cinema.stats().unusedWorldHdrWidth,1);
const lite=draws[0].target,parked=disposals;
render({reducedEffects:true});assert.equal(disposals,parked,'stable reduced profile must not reallocate per frame');
assert.equal(hdr.width,1);assert.equal(lite.width,350);
assert.deepEqual(render(),{calls:16,world:2,clears:1});
assert.ok(disposals>parked,'restoring 4x HDR reallocates and can hitch once');
assert.equal(cinema.stats().worldMsaaSamples,4);assert.equal(cinema.stats().bloomPasses,3);assert.equal(cinema.stats().contactOcclusionSamples,8);
assert.equal(draws[0].target,hdr);assert.equal(hdr.width,1400);assert.equal(hdr.height,900);
assert.equal(cinema.stats().liveWorldScale,1);
assert.equal(lite.width,1);assert.equal(lite.height,1);
assert.deepEqual(render({exterior:true}),{calls:11,world:2,clears:0});assert.deepEqual(render({exterior:true,reducedEffects:true}),{calls:3,world:2,clears:0});
assert.equal(hdr.width,1);assert.equal(cinema.stats().unusedWorldHdrWidth,1);
assert.deepEqual(render({worldOnly:true,reducedEffects:true}),{calls:2,world:1,clears:0});
const afterReduced=disposals;
cinema.captureStingFrame();const frozen=cinema.stingSnapshot().target,serial=cinema.stingSnapshot().serial;cinema.contextLost();cinema.recoverContext();render({reducedEffects:true});assert.equal(cinema.stingSnapshot().target,frozen);assert.ok(cinema.stingSnapshot().serial>serial);assert.equal(cinema.stats().worldMsaaSamples,0);assert.ok(disposals>afterReduced);assert.equal(hdr.samples,4);assert.equal(hdr.width,1);
console.log('PASS full/reduced/exterior/world-only render paths, quarter-res reduced world HDR, parked unused 4x HDR, 0-sample twin, restore hitch, and sting context recovery. Interior reduced mode is world+cockpit+composite.');
